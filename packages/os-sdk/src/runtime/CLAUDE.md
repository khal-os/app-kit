# Runtime — Pluggable backend abstraction
Each file is one runtime implementation. `local.ts` and `remote.ts` are live. Everything else is a typed stub waiting for implementation. Add new cloud providers by extending `BaseRuntime`.
